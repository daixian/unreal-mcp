* 要理解代码然后加上中文注释.
* C++代码注释使用Doxygen-style.必须要使用 @brief,有参数必须使用 @param .灵活使用 @note @par @code{.cpp} @endcode @see 等等这些.
* @param 必须要加一个方向属性，合法值是 [in]、[out]、[in,out]。
* 可以补充原有的注释内容，但是注意千万不要简化原有的注释内容.
* 类的私有成员也要添加注释.
* 检查代码的隐患错误,加上注释todo之类的的.但是不要改动原始代码.