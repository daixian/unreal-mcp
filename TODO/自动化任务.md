- 需要按 `Plugins\UnrealMCP\TODO\本地Python实现功能列表.md` 中的“实现层决策规则”推进，不再把“C++ 到 Python 迁移”当作默认目标。
- 对每个候选能力先判断应落在 `本地Python`、`Python + C++桥接`、还是 `C++主实现`，再实施、编译、测试和记录。
- 已完成迁移或改造的功能，需要同步更新 `常用的UE功能列表.md` 与 `本地Python实现功能列表.md` 中对应条目的状态、实现层说明和记录。
- 需要编译、启动测试、有错误就修复；本轮目标是持续清理清单，而不是机械完成 `本地Python实现功能列表.md` 中全部 Python 化条目。
- 完成之后总结一个新的报告(命名`YYYY-MM-DD HH:mm:ss`),写到 `Agent\Report\自动化` 目录夹中.
- `Agent\Report\自动化`中查找最近的报告,分析之前的报告中未完成的任务,可以作为这次任务的参考.
